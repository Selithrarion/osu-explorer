{
  "name": "osu-explorer-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm tauri dev",
    "build": "pnpm tauri build",
    "build:backend:linux": "cd backend && cross build --target x86_64-unknown-linux-gnu --release"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.5"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "~2.4.2"
  }
}